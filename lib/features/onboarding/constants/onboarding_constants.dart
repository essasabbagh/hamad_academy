import 'package:flutter/material.dart';

const Duration onboardingDuration = Durations.medium4;
const Duration onboardingTimerDuration = Duration(seconds: 10);
const Curve onboardingCurves = Curves.easeInOutCubic;

const double maxSize = 12;
const double minSize = 8;
