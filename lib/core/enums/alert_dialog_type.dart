/// Defines the type of the alert dialog, affecting its icon and colors.
enum AlertDialogType {
  /// Represents an error dialog with a red theme.
  error,

  /// Represents a warning dialog with an orange theme.
  warning,

  /// Represents an informational dialog with a primary theme color.
  info,

  /// Represents a success dialog with a green theme.
  success,
}
